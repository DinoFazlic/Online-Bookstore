<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Dashboard</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: linear-gradient(to bottom, #e3f2fd, #ffffff);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }
        .navbar {
            padding: 15px;
        }
        .btn-logout {
            border-radius: 50px;
        }
        .btn-credits {
            font-weight: bold;
            border-radius: 50px;
        }
        .welcome-section {
            margin-top: 80px;
        }
        .action-buttons a {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .action-buttons a:hover {
            transform: scale(1.05);
            box-shadow: 0px 6px 15px rgba(0, 0, 0, 0.2);
        }
        .footer {
            background-color: #f8f9fa;
            text-align: center;
            padding: 10px 0;
            margin-top: auto;
            border-top: 1px solid #dee2e6;
        }
    </style>
</head>
<body>

<!-- Header Section -->
<header class="navbar navbar-expand-lg navbar-dark bg-primary shadow">
    <div class="container-fluid">
        <a href="/auth/logout" class="btn btn-danger btn-sm btn-logout">Logout</a>
        <a href="/users/topup" class="btn btn-success btn-sm ms-auto btn-credits">Credits: $<%= user.credits %></a>
    </div>
</header>

<!-- Welcome Section -->
<div class="container welcome-section text-center py-5">
    <h1 class="display-4 mb-4 text-primary fw-bold">Welcome, <%= user.username %>!</h1>
    <p class="text-secondary mb-5">Manage your books and discover new ones in our library!</p>
    <div class="row justify-content-center action-buttons">
        <!-- Action Buttons -->
        <div class="col-md-4 mb-3">
            <a href="/users/shop" class="btn btn-outline-primary btn-lg w-100 py-3 shadow">Buy Books</a>
        </div>
        <div class="col-md-4">
            <a href="/users/myBooks" class="btn btn-outline-secondary btn-lg w-100 py-3 shadow">My Books</a>
        </div>
    </div>
</div>

<!-- Footer -->
<footer class="footer">
    <p class="mb-0 text-muted">&copy; <%= new Date().getFullYear() %> Online Bookstore. All rights reserved.</p>
</footer>

<!-- Bootstrap JS -->
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
